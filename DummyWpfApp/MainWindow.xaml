<Window x:Class="DummyWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dummy Application" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Status:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="5">
            <ComboBoxItem Content="Active" IsSelected="True"/>
            <ComboBoxItem Content="Inactive"/>
            <ComboBoxItem Content="Pending"/>
        </ComboBox>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <Button Content="OK" Width="75" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="75"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" Text="Sample WPF Window for PNG rendering test." 
                   VerticalAlignment="Bottom" HorizontalAlignment="Center" Foreground="Gray"/>
    </Grid>
</Window>